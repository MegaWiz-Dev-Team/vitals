[package]
publish = false   # internal workspace crate, never to crates.io
name = "vitals-osce"
description = "Deterministic OSCE rubric scorer — the re-derivable part of an exam score (det_40)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
vitals-sce = { path = "../vitals-sce" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true

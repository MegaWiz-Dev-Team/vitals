[package]
name = "vitals-sce"
description = "Independent implementation of the Vitals scenario semantics — schema and interpreter"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"

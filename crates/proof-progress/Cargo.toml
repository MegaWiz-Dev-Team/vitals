[package]
name = "proof-progress"
description = "Integer twin of Embla's competency arithmetic — the copy that runs onchain"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = ["std"]
std = []

[dependencies]

target/
.git/
state/
# The deck is compiled into the binary, so it has to reach the build. Everything else in pitch/
# is renders, video, and the speaking notes — none of which belong inside a served image.
pitch/*
!pitch/deck.html
docs/
demo/clips/
**/*.mp4
